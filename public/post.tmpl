{{ define "header" }}
  <a href="/" title="Retour à l'accueil"><img class="blogLogo blogLogo--small" src="/images/logo.png"></a>
{{ end }}

{{ define "content" }}
<article>
  <h2 class="postTitle">
    <img src="/images/flags/{{ .Country }}.svg" class="flag">
    {{ .Title }}
  </h2>
  <time class="postTime">{{ format .Date }}</time>

  {{ .Content }}
</article>
{{ end }}

{{ define "scripts" }}
<script>
var viewer = new Viewer(document.querySelector('article'), {
  movable: false,
  zoomable: false,
  rotatable: false,
  scalable: false,
  transition: false

});
</script>
{{ end }}
